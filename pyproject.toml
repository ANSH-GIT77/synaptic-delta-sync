[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "synaptic-delta-sync"
version = "0.1.0"
authors = [
  { name="Ansh Nimbalkar" }
]
description = "A production-grade, secure, atomic neural weight injection library."
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "torch>=2.0.0",
]

[project.urls]
Homepage = "https://github.com/ANSH-GIT77/synaptic-delta-sync"